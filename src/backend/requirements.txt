# PyReflect Interface Backend
# NOTE: Prefer `uv sync` with pyproject.toml. This file is for pip compatibility.

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
numpy>=2.1.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
redis>=5.0.0
rq>=1.15.0
requests>=2.31.0
pymongo>=4.6.0
huggingface-hub>=0.20.0
typer>=0.9.0

# Needed for instant Modal worker spawn from the backend (optional but recommended)
modal>=0.64.0

# PyReflect from GitHub (pulls in torch, pandas, scipy, scikit-learn, refl1d, refnx, etc.)
pyreflect @ git+https://github.com/williamQyq/pyreflect.git@main
